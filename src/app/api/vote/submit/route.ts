import { bumpLiveVersion } from "@/lib/live-version";
import { invalidateStatsCache } from "@/lib/stats";
import { NextRequest,NextResponse } from "next/server";
import { adminDb } from "@/lib/firebase-admin";
import { verifyVoteToken } from "@/lib/session";
import { randomUUID } from "crypto";
export async function POST(req:NextRequest){let token:string|undefined,candidatId:string|null;try{const b=await req.json();token=b.token;candidatId=b.candidatId??null}catch{return NextResponse.json({ok:false,error:"requete_invalide"},{status:400})}const s=verifyVoteToken(token);if(!s)return NextResponse.json({ok:false,error:"session_invalide"},{status:401});const eRef=adminDb.collection("electeurs").doc(s.electeurId),scRef=adminDb.collection("scrutins").doc(s.scrutinId),pRef=adminDb.collection("participations").doc(`${s.scrutinId}__${s.electeurId}`),cRef=candidatId?adminDb.collection("candidats").doc(candidatId):null;try{await adminDb.runTransaction(async tx=>{const [e,sc,p,c]=await Promise.all([tx.get(eRef),tx.get(scRef),tx.get(pRef),cRef?tx.get(cRef):Promise.resolve(null)]);if(!e.exists)throw new Error("matricule_introuvable");if(!sc.exists||sc.data()?.statut!=="OUVERT")throw new Error(sc.data()?.statut==="SUSPENDU"?"scrutin_suspendu":"scrutin_ferme");if(p.exists)throw new Error("deja_vote");if(cRef&&(!c?.exists||!c.data()?.actif||c.data()?.scrutinId!==s.scrutinId))throw new Error("candidat_invalide");const date=new Date().toISOString();const voteRef=adminDb.collection("votes").doc(randomUUID());tx.set(voteRef,{scrutinId:s.scrutinId,candidatId,date});tx.set(pRef,{scrutinId:s.scrutinId,electeurId:s.electeurId,dateVote:date});tx.set(adminDb.collection("voteRevocations").doc(pRef.id),{scrutinId:s.scrutinId,voteId:voteRef.id,createdAt:date});tx.set(eRef,{aVote:true,dateVote:date},{merge:true});bumpLiveVersion(tx,"vote_enregistre");});}catch(e){const m=e instanceof Error?e.message:"erreur_inconnue";return NextResponse.json({ok:false,error:m},{status:409})}invalidateStatsCache();return NextResponse.json({ok:true});}
